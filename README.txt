log day 1 :

HomePage.jsx is the file used by react to render the contents it is in the pages folder because it needs to be separate
from the client and server code.

Importing is very important because that's how you find your files from directories

pages contains all the jsx files for react to build the classes and render on. Add all new pages there. 
- you need to update main.html and main.js also to accommodate these changes. 

feature 1 - Render components

feature 2 - Setup routing